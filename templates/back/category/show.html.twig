{% extends 'base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
    <h1>Catégorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_category_index') }}">Retour à la liste</a>

      <a class="btn btn-primary btn-sm" href="{{ path('back_category_edit', {'id': category.id}) }}">édition</a>

    {{ include('back/category/_delete_form.html.twig') }}
{% endblock %}
