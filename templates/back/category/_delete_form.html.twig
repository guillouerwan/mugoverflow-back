<form method="post" action="{{ path('back_category_delete', {'id': category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cette catégorie');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
