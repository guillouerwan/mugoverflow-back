{% extends 'base.html.twig' %}

{% block title %}Couleur secondaire{% endblock %}

{% block body %}
    <h1>Couleur secondaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ secondary_color.id }}</td>
            </tr>
            <tr>
                <th>Hexa de la couleur secondaire</th>
                <td>{{ secondary_color.secondaryHexa }}</td>
            </tr>
            <tr>
                <th>Nom de la couleur secondaire</th>
                <td>{{ secondary_color.secondaryColorName }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ secondary_color.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_secondary_color_index') }}">Retour à la liste</a>

    <a href="{{ path('back_secondary_color_edit', {'id': secondary_color.id}) }}">éditer</a>

    {{ include('back/secondary_color/_delete_form.html.twig') }}
{% endblock %}
