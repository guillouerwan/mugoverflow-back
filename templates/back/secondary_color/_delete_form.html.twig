<form method="post" action="{{ path('back_secondary_color_delete', {'id': secondary_color.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secondary_color.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
