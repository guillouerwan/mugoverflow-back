{% extends 'base.html.twig' %}

{% block title %}Edition du status : {{ status.name }}{% endblock %}

{% block body %}
    <h1>Edition du status : {{ status.name }}</h1>

    {{ include('back/status/_form.html.twig', {'button_label': 'Mise à jour'}) }}
    <div class="d-flex justify-content-between mt-2">
    {{ include('back/status/_delete_form.html.twig') }}
    <a href="{{ path('back_status_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
