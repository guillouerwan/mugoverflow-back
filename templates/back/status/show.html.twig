{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <h1>Statut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_status_index') }}">back to list</a>

    <a href="{{ path('back_status_edit', {'id': status.id}) }}">edit</a>

    {{ include('back/status/_delete_form.html.twig') }}
{% endblock %}
