{% extends 'base.html.twig' %}

{% block title %}Reset mot de passe{% endblock %}

{% block body %}
    <h1>Reset mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Reset mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
